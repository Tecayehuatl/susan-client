import { GetPaymentTypeTextPipe } from './get-payment-type-text.pipe';

describe('GetPaymentTypeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPaymentTypeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
