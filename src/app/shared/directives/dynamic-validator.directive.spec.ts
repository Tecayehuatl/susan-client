import { DynamicValidatorDirective } from './dynamic-validator.directive';

describe('DynamicValidatorDirective', () => {
    it('should create an instance', () => {
        const directive = new DynamicValidatorDirective();
        expect(directive).toBeTruthy();
    });
});
